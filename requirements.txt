# Runtime dependencies — **locked** after the bootstrap-mcp-server spike
# confirmed stdio round-trip works against codebuddy and Copilot (via the
# Python MCP client used by both). See:
#   openspec/changes/bootstrap-mcp-server/design.md
#
# Version bumps must come with a re-run of the integration tests and an
# updated ``mcp.server.fastmcp.FastMCP`` compatibility note in design.md.

mcp==1.29.0
pydantic>=2.9,<3
psutil>=6.0,<7
pywin32>=306; sys_platform == "win32"
